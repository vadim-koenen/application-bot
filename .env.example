APPLICATION_BOT_DB=
LIVE_APPLY_ENABLED=false
LIVE_EMAIL_SEND_ENABLED=false
EMAIL_SEND_APPROVAL_PHRASE=
# SMTP for send-digest --live and gated email-apply. For Gmail use an App
# Password (Account > Security > 2FA > App passwords): host smtp.gmail.com, port 587.
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=
# Where the apply digest is sent (your own inbox).
DIGEST_TO=
GREENHOUSE_API_KEY=
LEVER_API_KEY=
ASHBY_API_KEY=
# Adzuna market-wide search (free key at https://developer.adzuna.com/).
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
